[project]
name = "fantasy-baseball-drafter"
version = "0.1.0"
description = "AI-powered fantasy baseball draft optimizer"
requires-python = ">=3.12"
dependencies = [
    "pandas",
    "openpyxl",
]

[project.scripts]
drafter = "drafter.cli:main"
